/** @deprecated Prefer AppQuotePreview from VoiceQuoteDemoScreen — kept for import stability. */
export { AppQuotePreview as QuoteResult, APP_PREVIEW_QUOTE as DEMO_PREVIEW_QUOTE } from './VoiceQuoteDemoScreen';
export const DEMO_PREVIEW_TRANSCRIPT = 'Kitchen tap replacement for Sarah Jones, call-out and travel';
